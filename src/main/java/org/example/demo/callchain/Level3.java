package org.example.demo.callchain;

import org.apache.commons.lang3.StringUtils;

public class Level3 {
    // 混合复杂场景
    public Boolean level3_func8(boolean arg1) {
        return StringUtils.isBlank(null);
    }
}
